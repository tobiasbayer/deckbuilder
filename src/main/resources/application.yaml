spring:
  application:
    name: deckbuilder

langchain4j:
  open-ai:
    chat-model:
      api-key: ${OPENAI_API_KEY}
      model-name: gpt-5.4-mini
      temperature: 0.7
      log-requests: true
      log-responses: true

scryfall:
  base-url: https://api.scryfall.com